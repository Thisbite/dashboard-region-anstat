CREATE TABLE regions (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nom_region VARCHAR(100),
    description TEXT,
    image_url VARCHAR(255),
    population_estimee VARCHAR(100),
    lien VARCHAR(255)
);


INSERT INTO regions (nom_region, description, image_url, population_estimee, lien) VALUES
('Région d\'Abidjan', 'Région située au sud de la CI avec population estimée à plus de 4 millions d\'habitants en 2021', 'img/abidjan2.jpeg', '4 millions', '#'),
('Région du Tchologo', 'Région située au nord de la CI avec une population estimée à plus de 4 millions d\'habitants en 2021', 'img/tchol3.jpeg', '4 millions', '#'),
('Région de Tonpki', 'Région située à l\'ouest de la CI avec une population estimée à plus de 4 millions d\'habitants en 2021', 'img/man1.jpeg', '4 millions', '#'),
('Région de Yamoussoukro', 'Région située au centre de la CI avec une population estimée à plus de 4 millions d\'habitants en 2021', 'img/yamoussous1.jpeg', '4 millions', '#');


@app.route('/dashboard_regions')
def dashboard_regions():
    cursor = mysql.connection.cursor()
    cursor.execute("SELECT * FROM regions")
    regions = cursor.fetchall()
    return render_template('dashboard_regions.html', regions=regions)


<div class="row row-cols-1 row-cols-4 g-4 py-5">
    {% for region in regions %}
    <div class="col">
        <div class="card" style="width: 18rem;">
            <img src="{{ url_for('static', filename=region['image_url']) }}" class="card-img-top" alt="{{ region['nom_region'] }}">
            <div class="card-body">
                <h5 class="card-title">{{ region['nom_region'] }}</h5>
                <p class="card-text">{{ region['description'] }}</p>
                <a href="{{ region['lien'] }}" class="btn btn-primary">Découvrir plus</a>
            </div>
        </div>
    </div>
    {% endfor %}
</div>
